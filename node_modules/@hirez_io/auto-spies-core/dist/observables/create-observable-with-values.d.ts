import { Observable, ReplaySubject } from 'rxjs';
import { ValueConfig } from '..';
export declare function createObservableWithValues<T>(valuesConfigs: ValueConfig<T>[]): Observable<T>;
export declare function createObservableWithValues<T>(valuesConfigs: ValueConfig<T>[], config: {
    returnSubject: true;
}): {
    values$: Observable<T>;
    subject: ReplaySubject<T>;
};
